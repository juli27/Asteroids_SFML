#pragma once

//TODO: rename?
// root class from which the UI is created, managed and drawn.
// tree like structure (similar to JavaFX) but refs of components are stored here in a container
// to avoid countless function calls to traverse the tree (not required because only container can
// have children?)
class UIRoot {
private:

public:
};
